-- test comment at the start of the line

CREATE TABLE table_name ( -- test comment at the end of the line
    column_id INT NOT NULL,
    column_value VARCHAR NOT NULL,
    PRIMARY KEY (column_id)
);